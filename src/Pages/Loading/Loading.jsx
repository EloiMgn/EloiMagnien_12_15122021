import './Loading.scss';

/**
 * Return react component Loading page until the data have been fetched
 * @returns { JSX.Element } 
 */
const Error = () => (
  <div id="loading"></div>
);

export default Error;